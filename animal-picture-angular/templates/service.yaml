apiVersion: v1
kind: Service
metadata:
  name: {{ include "animal-picture-angular.fullname" . }}
  labels:
    {{- include "animal-picture-angular.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "animal-picture-angular.selectorLabels" . | nindent 4 }}
