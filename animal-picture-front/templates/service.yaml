apiVersion: v1
kind: Service
metadata:
  name: {{ include "animal-picture-front.fullname" . }}
  labels:
    {{- include "animal-picture-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "animal-picture-front.selectorLabels" . | nindent 4 }}
