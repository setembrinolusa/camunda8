apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "animal-picture-front.fullname" . }}-test-connection"
  labels:
    {{- include "animal-picture-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "animal-picture-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
