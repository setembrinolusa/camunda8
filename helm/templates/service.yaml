apiVersion: v1
kind: Service
metadata:
  name: {{ include "animal-picture-worker.fullname" . }}
  labels:
    {{- include "animal-picture-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "animal-picture-worker.selectorLabels" . | nindent 4 }}
